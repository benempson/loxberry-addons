---
phase: 06-auto-update-mechanism
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin.cfg
  - release.cfg
  - release.js
  - .github/workflows/release.yml
  - .gitattributes
autonomous: true
requirements:
  - UPDT-01
  - UPDT-02
  - UPDT-03

must_haves:
  truths:
    - "Loxberry can fetch release.cfg from the raw GitHub URL and detect available updates"
    - "Creating a GitHub Release triggers automatic zip build and asset upload"
    - "Running release.js updates release.cfg alongside the other version files"
    - "User config and data are preserved across auto-updates (existing preupgrade.sh/postinstall.sh)"
  artifacts:
    - path: "release.cfg"
      provides: "Loxberry auto-update manifest with VERSION, ARCHIVEURL, INFOURL"
      contains: "[AUTOUPDATE]"
    - path: "plugin.cfg"
      provides: "AUTOUPDATE section pointing to raw GitHub release.cfg URL"
      contains: "AUTOMATIC_UPDATES=true"
    - path: "release.js"
      provides: "Version bumper that also updates release.cfg"
      contains: "release.cfg"
    - path: ".github/workflows/release.yml"
      provides: "GitHub Actions workflow that builds zip and uploads to release"
      contains: "softprops/action-gh-release"
  key_links:
    - from: "plugin.cfg"
      to: "release.cfg"
      via: "RELEASECFG raw GitHub URL"
      pattern: "RELEASECFG=https://raw.githubusercontent.com"
    - from: "release.cfg"
      to: "GitHub Releases"
      via: "ARCHIVEURL download link"
      pattern: "ARCHIVEURL=https://github.com/benempson/loxberry-addons/releases/download"
    - from: ".github/workflows/release.yml"
      to: "release.cfg"
      via: "Workflow updates release.cfg after uploading zip"
      pattern: "release.cfg"
---

<objective>
Configure Loxberry's built-in auto-update system so the plugin updates itself from GitHub Releases without manual packaging.

Purpose: Eliminate the manual download-and-reinstall workflow. After one manual install of this version, all future updates are automatic.
Output: release.cfg manifest, updated plugin.cfg AUTOUPDATE section, enhanced release.js, GitHub Actions release workflow.
</objective>

<execution_context>
@C:/Users/Ben/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-auto-update-mechanism/06-RESEARCH.md

<interfaces>
<!-- Current plugin.cfg AUTOUPDATE section (to be modified): -->
[AUTOUPDATE]
AUTOMATIC_UPDATES=false
RELEASECFG=
PRERELEASECFG=

<!-- Current release.js builds zip and commits version bumps to: -->
<!-- package.json, bin/package.json, plugin.cfg -->
<!-- Zip built via: git archive --format=zip --output={zipName} HEAD {files} -->
<!-- Zip naming: zigbee-watchdog-{version}.zip -->

<!-- Current version: 0.6.2 -->

<!-- Current .gitattributes: -->
*.sh text eol=lf
uninstall/uninstall text eol=lf

<!-- Files included in git archive zip: -->
<!-- plugin.cfg preinstall.sh preupgrade.sh postinstall.sh uninstall/ bin/ webfrontend/ templates/ README.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create release.cfg, update plugin.cfg, and enhance release.js</name>
  <files>release.cfg, plugin.cfg, release.js</files>
  <read_first>
    - plugin.cfg (current AUTOUPDATE section to modify)
    - release.js (current version bumper to extend)
    - package.json (current version: 0.6.2)
    - .planning/phases/06-auto-update-mechanism/06-RESEARCH.md (release.cfg format and plugin.cfg target values)
  </read_first>
  <action>
1. Create `release.cfg` at repo root with this exact content:
```ini
[AUTOUPDATE]
VERSION=0.6.2
ARCHIVEURL=https://github.com/benempson/loxberry-addons/releases/download/0.6.2/zigbee-watchdog-0.6.2.zip
INFOURL=https://github.com/benempson/loxberry-addons/releases
```

2. Update `plugin.cfg` [AUTOUPDATE] section to:
```ini
[AUTOUPDATE]
AUTOMATIC_UPDATES=true
RELEASECFG=https://raw.githubusercontent.com/benempson/loxberry-addons/main/release.cfg
PRERELEASECFG=https://raw.githubusercontent.com/benempson/loxberry-addons/main/prerelease.cfg
```
Leave all other sections ([AUTHOR], [PLUGIN], [SYSTEM]) unchanged.

3. Enhance `release.js` to also update `release.cfg` after bumping versions. After the existing `plugin.cfg` update block (around line 41), add:
```javascript
// Update release.cfg
const releaseCfg = `[AUTOUPDATE]\nVERSION=${newVersion}\nARCHIVEURL=https://github.com/benempson/loxberry-addons/releases/download/${newVersion}/zigbee-watchdog-${newVersion}.zip\nINFOURL=https://github.com/benempson/loxberry-addons/releases\n`;
fs.writeFileSync('release.cfg', releaseCfg);
console.log('Updated: release.cfg');
```
Also update the git add command (line 45) to include release.cfg:
```javascript
execSync('git add package.json bin/package.json plugin.cfg release.cfg', { stdio: 'inherit' });
```
And update the console log on line 42 to:
```javascript
console.log('Updated: package.json, bin/package.json, plugin.cfg, release.cfg');
```
  </action>
  <verify>
    <automated>grep -q "AUTOMATIC_UPDATES=true" plugin.cfg && grep -q "RELEASECFG=https://raw.githubusercontent.com/benempson/loxberry-addons/main/release.cfg" plugin.cfg && grep -q "VERSION=0.6.2" release.cfg && grep -q "ARCHIVEURL=https://github.com/benempson/loxberry-addons/releases/download/0.6.2/zigbee-watchdog-0.6.2.zip" release.cfg && grep -q "release.cfg" release.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - plugin.cfg contains `AUTOMATIC_UPDATES=true`
    - plugin.cfg contains `RELEASECFG=https://raw.githubusercontent.com/benempson/loxberry-addons/main/release.cfg`
    - plugin.cfg contains `PRERELEASECFG=https://raw.githubusercontent.com/benempson/loxberry-addons/main/prerelease.cfg`
    - release.cfg exists at repo root with `[AUTOUPDATE]` section
    - release.cfg contains `VERSION=0.6.2`
    - release.cfg contains `ARCHIVEURL=https://github.com/benempson/loxberry-addons/releases/download/0.6.2/zigbee-watchdog-0.6.2.zip`
    - release.cfg contains `INFOURL=https://github.com/benempson/loxberry-addons/releases`
    - release.js writes release.cfg during version bump (contains string `release.cfg`)
    - release.js git add command includes `release.cfg`
    - `npx jest` passes (existing tests unbroken)
  </acceptance_criteria>
  <done>release.cfg created with current version 0.6.2, plugin.cfg AUTOUPDATE section populated with raw GitHub URLs, release.js bumps release.cfg alongside other version files</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions release workflow and update .gitattributes</name>
  <files>.github/workflows/release.yml, .gitattributes</files>
  <read_first>
    - .gitattributes (current export-ignore rules)
    - release.js (git archive file list on line 50-53 to ensure workflow matches)
    - .planning/phases/06-auto-update-mechanism/06-RESEARCH.md (GitHub Actions workflow pattern)
  </read_first>
  <action>
1. Create `.github/workflows/release.yml` with this content:
```yaml
name: Release Plugin

on:
  release:
    types: [released]

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build plugin zip
        run: |
          TAG="${{ github.event.release.tag_name }}"
          ZIP_NAME="zigbee-watchdog-${TAG}.zip"
          git archive --format=zip --output="${ZIP_NAME}" HEAD \
            plugin.cfg preinstall.sh preupgrade.sh postinstall.sh \
            uninstall/ bin/ webfrontend/ templates/ README.md
          echo "ZIP_NAME=${ZIP_NAME}" >> $GITHUB_ENV

      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.ZIP_NAME }}

      - name: Update release.cfg
        run: |
          TAG="${{ github.event.release.tag_name }}"
          cat > release.cfg << EOF
          [AUTOUPDATE]
          VERSION=${TAG}
          ARCHIVEURL=https://github.com/benempson/loxberry-addons/releases/download/${TAG}/zigbee-watchdog-${TAG}.zip
          INFOURL=https://github.com/benempson/loxberry-addons/releases
          EOF
          # Remove leading whitespace from heredoc
          sed -i 's/^          //' release.cfg

      - name: Commit release.cfg
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add release.cfg
          git diff --cached --quiet && echo "No changes to commit" || git commit -m "chore: update release.cfg for ${{ github.event.release.tag_name }}"
          git push origin HEAD:main
```

2. Update `.gitattributes` to exclude CI/planning files from the git archive zip:
```
*.sh text eol=lf
uninstall/uninstall text eol=lf

# Exclude from plugin zip (git archive)
.github/ export-ignore
.planning/ export-ignore
.gitattributes export-ignore
.gitignore export-ignore
release.js export-ignore
jest.config.js export-ignore
tests/ export-ignore
node_modules/ export-ignore
coverage/ export-ignore
*.log export-ignore
```
This ensures `git archive` produces a clean plugin zip without dev/CI files.
  </action>
  <verify>
    <automated>test -f .github/workflows/release.yml && grep -q "softprops/action-gh-release" .github/workflows/release.yml && grep -q "export-ignore" .gitattributes && grep -q ".github/ export-ignore" .gitattributes && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/release.yml` exists
    - Workflow triggers on `release` event type `released`
    - Workflow has `permissions: contents: write`
    - Workflow uses `actions/checkout@v4`
    - Workflow runs `git archive` with same file list as release.js
    - Workflow uses `softprops/action-gh-release@v2` to upload zip
    - Workflow updates `release.cfg` with tag version and commits to main
    - `.gitattributes` contains `.github/ export-ignore`
    - `.gitattributes` contains `.planning/ export-ignore`
    - `.gitattributes` contains `release.js export-ignore`
    - `.gitattributes` contains `tests/ export-ignore`
    - `.gitattributes` preserves existing `*.sh text eol=lf` rule
    - `npx jest` passes (existing tests unbroken)
  </acceptance_criteria>
  <done>GitHub Actions workflow created that builds zip and uploads to release on publish, .gitattributes updated to exclude dev files from plugin archive</done>
</task>

</tasks>

<verification>
1. `grep -q "AUTOMATIC_UPDATES=true" plugin.cfg` -- AUTOUPDATE enabled
2. `test -f release.cfg` -- release manifest exists
3. `grep -q "VERSION=0.6.2" release.cfg` -- correct current version
4. `test -f .github/workflows/release.yml` -- CI workflow exists
5. `grep -q "export-ignore" .gitattributes` -- dev files excluded from archive
6. `npx jest` -- all existing tests still pass
</verification>

<success_criteria>
- Loxberry auto-update system fully configured: plugin.cfg points to release.cfg, release.cfg contains current version and download URL
- Release workflow automated: creating a GitHub Release triggers zip build and asset upload
- Version management consistent: release.js updates release.cfg alongside package.json, bin/package.json, and plugin.cfg
- Dev files excluded from plugin zip via .gitattributes export-ignore rules
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-auto-update-mechanism/06-01-SUMMARY.md`
</output>
