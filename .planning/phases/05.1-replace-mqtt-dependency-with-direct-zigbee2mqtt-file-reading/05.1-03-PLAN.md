---
phase: 05.1-replace-mqtt-dependency-with-direct-zigbee2mqtt-file-reading
plan: 03
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - webfrontend/htmlauth/index.php
  - templates/lang/language_en.ini
  - postinstall.sh
  - bin/verify-z2m.js
autonomous: true
requirements:
  - Z2M-06

must_haves:
  truths:
    - "Web UI shows Z2M Data Path field instead of MQTT connection settings"
    - "Verify Path button replaces Test MQTT Connection button"
    - "Verify Path button runs verify-z2m.js and shows device count + state.json freshness"
    - "Drain seconds field removed from UI"
    - "postinstall.sh default config has Z2M section instead of MQTT"
    - "Language file has Z2M labels instead of MQTT labels"
  artifacts:
    - path: "webfrontend/htmlauth/index.php"
      provides: "Updated config UI with z2m path field"
    - path: "bin/verify-z2m.js"
      provides: "CLI script to verify z2m data path (replaces test-mqtt.js)"
    - path: "templates/lang/language_en.ini"
      provides: "Updated language strings"
    - path: "postinstall.sh"
      provides: "Updated default config template"
  key_links:
    - from: "webfrontend/htmlauth/index.php"
      to: "bin/verify-z2m.js"
      via: "exec('node ' . LBPBINDIR . '/verify-z2m.js')"
      pattern: "verify-z2m\\.js"
---

<objective>
Update the PHP web UI to replace MQTT settings with z2m data path configuration, create the verify-z2m.js CLI script, update language strings, and update postinstall.sh default config.

Purpose: Complete the user-facing side of the MQTT removal. Users see z2m path config instead of MQTT settings.
Output: Updated PHP UI, new verify script, updated language file, updated postinstall
</objective>

<execution_context>
@C:/Users/Ben/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-replace-mqtt-dependency-with-direct-zigbee2mqtt-file-reading/05.1-CONTEXT.md
@.planning/phases/05.1-replace-mqtt-dependency-with-direct-zigbee2mqtt-file-reading/05.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: z2m-reader functions used by verify-z2m.js -->
From bin/lib/z2m-reader.js:
```javascript
function readZ2mState(z2mDataPath)    // returns { friendlyName: { ... }, ... }
function readZ2mDatabase(z2mDataPath) // returns [ { id, type, ieee_addr, ... }, ... ]
function detectZ2mPath()              // returns string path or null
```

From bin/lib/config.js (updated):
```javascript
// DEFAULTS.Z2M = { z2m_data_path: '' }
// No MQTT section
// CRON has no drain_seconds
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verify-z2m.js and update PHP UI, language file, and postinstall</name>
  <files>
    bin/verify-z2m.js,
    webfrontend/htmlauth/index.php,
    templates/lang/language_en.ini,
    postinstall.sh
  </files>
  <action>
    **1. Create bin/verify-z2m.js** (replaces bin/test-mqtt.js):
    - Shebang: `#!/usr/bin/env node`
    - 'use strict'
    - Hard timeout 10s (same pattern as old test-mqtt.js)
    - Read config to get z2m_data_path. If empty, call detectZ2mPath(). If still null, stderr "Z2M data path not found" and exit 1.
    - Try readZ2mState(path): count keys = device count from state.json
    - Try readZ2mDatabase(path): count entries (filter Coordinator) = device count from database.db
    - Get state.json mtime: `fs.statSync(path.join(z2mPath, 'state.json')).mtime`, compute age in seconds
    - Format age as human-readable (e.g., "2m ago", "3h ago")
    - stdout: `Z2M path verified: {path}\n{dbCount} devices in database, {stateCount} devices in state, state.json {age}\n`
    - exit 0 on success, exit 1 on any error (with stderr message)

    **2. Update templates/lang/language_en.ini:**
    - Replace MQTT labels:
      - `SECTION_MQTT` -> `SECTION_Z2M="Zigbee2MQTT Data"`
      - Remove: MQTT_HOST, MQTT_PORT, MQTT_TOPIC, MQTT_USER, MQTT_PASS
      - Add: `Z2M_PATH="Data Path"`, `Z2M_PATH_HELP="Path to zigbee2mqtt data directory (auto-detected if empty)"`, `Z2M_STATUS="Status"`
    - Remove: `DRAIN_SECONDS`, `DRAIN_SECONDS_HELP`
    - Replace: `TEST_MQTT` -> `VERIFY_Z2M="Verify Path"`
    - Keep all other strings unchanged

    **3. Update webfrontend/htmlauth/index.php:**

    a. **$defaults array:** Replace `'MQTT' => array(...)` with `'Z2M' => array('z2m_data_path' => '')`. Remove `drain_seconds` from CRON defaults.

    b. **collect_form_config():** Replace MQTT field collection with:
       ```php
       'Z2M' => array(
           'z2m_data_path' => trim($_POST['z2m_path'] ?? ''),
       ),
       ```
       Remove drain_seconds from CRON collection.

    c. **POST handler 'test_mqtt'** -> rename to **'verify_z2m':**
       Replace `exec('node ' . LBPBINDIR . '/test-mqtt.js 2>&1', ...)` with `exec('node ' . LBPBINDIR . '/verify-z2m.js 2>&1', ...)`
       Change `$test_result = array('type' => 'mqtt', ...)` to `array('type' => 'z2m', ...)`

    d. **POST handler 'save_settings' validation:**
       - Remove MQTT port validation
       - Remove drain_seconds validation
       - Keep all other validations (offline_hours, battery_pct, interval, smtp_port)

    e. **Settings tab HTML form:**
       Replace entire MQTT section (h3 + 5 field divs for host/port/topic/user/pass) with:
       ```php
       <h3><?php echo htmlspecialchars($L['SETTINGS.SECTION_Z2M']); ?></h3>
       <div data-role="fieldcontain">
           <label for="z2m_path"><?php echo htmlspecialchars($L['SETTINGS.Z2M_PATH']); ?></label>
           <input type="text" name="z2m_path" id="z2m_path"
                  value="<?php echo htmlspecialchars($config['Z2M']['z2m_data_path']); ?>"
                  placeholder="/opt/zigbee2mqtt/data">
           <p class="ui-body-d" style="font-size:0.85em;margin-top:2px;"><?php echo htmlspecialchars($L['SETTINGS.Z2M_PATH_HELP']); ?></p>
       </div>
       ```

    f. Remove drain_seconds field from Cron section.

    g. **Test buttons:** Replace "Test MQTT Connection" button with "Verify Path" button:
       Change `onclick` to set form-action to 'verify_z2m', update button id and label reference.
       Keep "Send Test Email" button as-is.

    h. **Test result display:** Update the condition `$test_result['type'] === 'mqtt'` to `=== 'z2m'` and label to "Z2M Path Verify".

    i. **JS:** Remove `togglePassword('mqtt_pass')` references (no more MQTT password field). Keep smtp password toggle and everything else.

    **4. Update postinstall.sh:**
    Replace the default config heredoc:
    - Remove `[MQTT]` section (host, port, base_topic, username, password)
    - Add `[Z2M]` section with `z2m_data_path =` (empty -- auto-detect at runtime)
    - Remove `drain_seconds` from `[CRON]` section
    - Keep all other sections identical
  </action>
  <verify>
    <automated>grep -c "mqtt\|MQTT" webfrontend/htmlauth/index.php bin/verify-z2m.js templates/lang/language_en.ini postinstall.sh 2>/dev/null; echo "---"; grep -c "z2m\|Z2M" webfrontend/htmlauth/index.php bin/verify-z2m.js 2>/dev/null; echo "---"; test -f bin/verify-z2m.js && echo "verify-z2m.js exists" || echo "MISSING"</automated>
  </verify>
  <done>
    - PHP UI shows Z2M Data Path field, no MQTT fields
    - Verify Path button calls verify-z2m.js
    - Drain seconds field removed from UI
    - Language file updated with Z2M strings, MQTT strings removed
    - postinstall.sh default config has Z2M section, no MQTT section
    - verify-z2m.js exists and uses z2m-reader module
    - No MQTT references remain in any of these files
  </done>
</task>

</tasks>

<verification>
```bash
# No MQTT references in updated files
grep -rn "mqtt\|MQTT" webfrontend/htmlauth/index.php templates/lang/language_en.ini postinstall.sh bin/verify-z2m.js | grep -v "zigbee2mqtt"
# Z2M references present
grep -c "z2m\|Z2M" webfrontend/htmlauth/index.php
# verify-z2m.js is executable
test -f bin/verify-z2m.js && echo OK
```
</verification>

<success_criteria>
- Web UI settings tab shows z2m data path field, no MQTT fields
- Verify Path button wired to verify-z2m.js
- No drain_seconds in UI
- Language file has z2m labels
- postinstall.sh default config updated
- Zero MQTT references in any modified file (except "zigbee2mqtt" as the product name)
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-replace-mqtt-dependency-with-direct-zigbee2mqtt-file-reading/05.1-03-SUMMARY.md`
</output>
