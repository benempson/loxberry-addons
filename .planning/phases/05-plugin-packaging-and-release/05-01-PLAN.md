---
phase: 05-plugin-packaging-and-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin.cfg
  - postinstall.sh
  - uninstall/uninstall
  - .gitattributes
  - bin/package.json
  - tests/cron-helper.test.js
  - bin/lib/cron-helper.js
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02
  - PLUG-03
must_haves:
  truths:
    - "Plugin has a valid plugin.cfg with correct metadata"
    - "postinstall.sh creates default config on first install and preserves config on upgrade"
    - "postinstall.sh runs npm install --production and registers cron job"
    - "uninstall script removes cron.d entry and clears notifications"
    - "Shell scripts use LF line endings (enforced via .gitattributes)"
    - "package.json exists in bin/ directory for npm install after Loxberry copies files"
  artifacts:
    - path: "plugin.cfg"
      provides: "Plugin metadata for Loxberry plugin manager"
      contains: "TITLE=Zigbee Device Monitor"
    - path: "postinstall.sh"
      provides: "Post-install lifecycle hook"
      contains: "npm install --production"
    - path: "uninstall/uninstall"
      provides: "Cleanup script for plugin removal"
      contains: "delete_notifications"
    - path: ".gitattributes"
      provides: "LF enforcement for shell scripts"
      contains: "eol=lf"
    - path: "bin/package.json"
      provides: "Dependencies manifest in bin/ for postinstall npm install"
      contains: "zigbee-watchdog"
    - path: "bin/lib/cron-helper.js"
      provides: "Shared interval-to-cron mapping logic"
      exports: ["intervalToCron"]
    - path: "tests/cron-helper.test.js"
      provides: "Tests for cron expression mapping"
      min_lines: 20
  key_links:
    - from: "postinstall.sh"
      to: "bin/lib/config.js"
      via: "Default config values must match DEFAULTS object"
      pattern: "interval_minutes = 60"
    - from: "postinstall.sh"
      to: "bin/package.json"
      via: "cd $PBIN && npm install --production"
      pattern: "npm install"
---

<objective>
Create the Loxberry plugin scaffold: plugin.cfg metadata, postinstall.sh lifecycle script, uninstall script, .gitattributes for LF enforcement, and a bin/package.json copy for npm install after Loxberry file installation. Also create a shared cron-helper module with TDD for the interval-to-cron mapping used by both postinstall.sh and PHP.

Purpose: Establish the complete plugin directory structure so the plugin can be installed, upgraded, and uninstalled on a Loxberry host.
Output: plugin.cfg, postinstall.sh, uninstall/uninstall, .gitattributes, bin/package.json, bin/lib/cron-helper.js, tests/cron-helper.test.js
</objective>

<execution_context>
@C:/Users/Ben/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-plugin-packaging-and-release/05-RESEARCH.md
@.planning/phases/05-plugin-packaging-and-release/05-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From bin/lib/config.js (DEFAULTS must match postinstall default config):
```javascript
const DEFAULTS = {
  MQTT: { host: 'localhost', port: '1883', base_topic: 'zigbee2mqtt', username: '', password: '' },
  THRESHOLDS: { offline_hours: '24', battery_pct: '25' },
  CRON: { interval_minutes: '60', drain_seconds: '3' },
  NOTIFICATIONS: { loxberry_enabled: '0', email_enabled: '0', smtp_host: '', smtp_port: '587', smtp_user: '', smtp_pass: '', smtp_from: '', smtp_to: '', heartbeat_enabled: '0' },
  EXCLUSIONS: { devices: '' },
};
```

From package.json (root):
```json
{
  "name": "zigbee-watchdog",
  "version": "0.1.0",
  "dependencies": {
    "ini": "^5.0.0",
    "mqtt": "^5.15.0",
    "nodemailer": "^8.0.2",
    "proper-lockfile": "^4.1.2",
    "write-file-atomic": "^7.0.0"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cron helper module with TDD (Wave 0 test scaffold + implementation)</name>
  <files>bin/lib/cron-helper.js, tests/cron-helper.test.js</files>
  <behavior>
    - intervalToCron(5) returns "*/5 * * * *"
    - intervalToCron(15) returns "*/15 * * * *"
    - intervalToCron(30) returns "*/30 * * * *"
    - intervalToCron(60) returns "0 * * * *"
    - intervalToCron(120) returns "0 */2 * * *"
    - intervalToCron(240) returns "0 */4 * * *"
    - intervalToCron(360) returns "0 */6 * * *"
    - intervalToCron(720) returns "0 */12 * * *"
    - intervalToCron(1440) returns "0 3 * * *"
    - Edge: intervalToCron(0) or negative treated as 60 (safe default)
    - String input: intervalToCron("30") works (coerces to number)
  </behavior>
  <action>
    RED: Create tests/cron-helper.test.js with all behavior cases above. Run tests -- they must fail (module not found).

    GREEN: Create bin/lib/cron-helper.js exporting `intervalToCron(minutes)`:
    - Parse minutes as integer (parseInt, default to 60 if NaN or <= 0)
    - If minutes < 60: return `*/${minutes} * * * *`
    - If minutes >= 60 and < 1440: hours = minutes/60, return `0 */${hours} * * *`
    - If minutes >= 1440: return `0 3 * * *` (daily at 3am)

    This module is shared: Node.js tests validate the logic, and the PHP `interval_to_cron()` function in Plan 02 must implement the same mapping.
  </action>
  <verify>
    <automated>npx jest --testPathPattern=cron-helper -x</automated>
  </verify>
  <done>All interval-to-cron mappings pass for all 9 preset intervals plus edge cases</done>
</task>

<task type="auto">
  <name>Task 2: Plugin metadata, lifecycle scripts, and packaging files</name>
  <files>plugin.cfg, postinstall.sh, uninstall/uninstall, .gitattributes, bin/package.json</files>
  <action>
    1. Create `plugin.cfg` (INI format, exact content from RESEARCH.md Pattern 1):
       - [AUTHOR] NAME=Ben Empson, EMAIL= (empty)
       - [PLUGIN] VERSION=0.1.0, NAME=zigbee_watchdog, FOLDER=zigbee_watchdog, TITLE=Zigbee Device Monitor
       - [AUTOUPDATE] AUTOMATIC_UPDATES=false, RELEASECFG=, PRERELEASECFG=
       - [SYSTEM] REBOOT=false, LB_MINIMUM=2.0.0, LB_MAXIMUM=false, ARCHITECTURE=false, CUSTOM_LOGLEVELS=false, INTERFACE=2.0

    2. Create `postinstall.sh` following the complete example in RESEARCH.md Pattern 2 + code example:
       - Parse arguments: ARGV1-ARGV5 (temp folder, plugin name, plugin folder, version, loxberry base)
       - Derive paths: PCONFIG, PDATA, PBIN, PLOG from $LBHOMEDIR
       - Step 1: mkdir -p "$PDATA" && chown loxberry:loxberry "$PDATA"
       - Step 2: If watchdog.cfg not exists in $PCONFIG, write default config matching bin/lib/config.js DEFAULTS exactly (all sections, all fields, all default values). chown loxberry:loxberry the config file.
       - Step 3: cd "$PBIN" && npm install --production 2>&1. If fails, echo WARNING but do NOT exit 2.
       - Step 4: Read interval_minutes from config (grep + sed), default 60. Build cron expression using same logic as cron-helper.js. Write to temp file, call $LBHOMEDIR/sbin/installcrontab.sh, cleanup temp file.
       - Step 5: Check `node --version` exists. If not, echo WARNING about Node.js being required.
       - Exit 0 on success. Never exit 2 for non-critical issues.

    3. Create `uninstall/uninstall` (no .sh extension):
       - #!/bin/bash
       - Remove cron.d entry: rm -f "$LBHOMEDIR/system/cron/cron.d/$1" 2>/dev/null
       - Clear notifications: source notify.sh, call delete_notifications "$1" watchdog
       - Guard notify.sh sourcing with -f check
       - Exit 0 always

    4. Create `.gitattributes` at project root:
       - *.sh text eol=lf
       - uninstall/uninstall text eol=lf
       - This prevents Windows CRLF from breaking shell scripts on the Loxberry host.

    5. Create `bin/package.json` -- copy of root package.json but with ONLY production fields:
       - name, version, private, dependencies (NO devDependencies, NO scripts, NO main)
       - This is what npm install --production uses after Loxberry copies bin/ to $PBIN

    IMPORTANT: All shell scripts MUST use Unix LF line endings. Use the Write tool which handles this.
    IMPORTANT: Default config in postinstall.sh must match bin/lib/config.js DEFAULTS exactly.
  </action>
  <verify>
    <automated>bash -n postinstall.sh && bash -n uninstall/uninstall && node -e "require('./bin/lib/cron-helper.js')" && npx jest -x</automated>
  </verify>
  <done>
    - plugin.cfg exists with correct metadata (TITLE=Zigbee Device Monitor, VERSION=0.1.0, INTERFACE=2.0)
    - postinstall.sh passes bash -n syntax check
    - uninstall/uninstall passes bash -n syntax check
    - .gitattributes enforces LF for shell scripts
    - bin/package.json contains production dependencies only
    - Full test suite still passes
  </done>
</task>

</tasks>

<verification>
- `bash -n postinstall.sh` -- shell syntax valid
- `bash -n uninstall/uninstall` -- shell syntax valid
- `npx jest -x` -- all tests pass including cron-helper
- `node -e "const c = require('./bin/lib/cron-helper.js'); console.log(c.intervalToCron(60))"` -- prints "0 * * * *"
- Verify plugin.cfg contains TITLE=Zigbee Device Monitor and INTERFACE=2.0
- Verify bin/package.json has no devDependencies
</verification>

<success_criteria>
- Plugin directory structure matches Loxberry conventions (plugin.cfg at root, postinstall.sh at root, uninstall/uninstall in uninstall/)
- postinstall.sh is idempotent (safe to run on fresh install and upgrades)
- uninstall script handles missing files gracefully (2>/dev/null, -f guards)
- Default config in postinstall.sh matches bin/lib/config.js DEFAULTS exactly
- All 9 cron interval presets have correct cron expressions (validated by test)
</success_criteria>

<output>
After completion, create `.planning/phases/05-plugin-packaging-and-release/05-01-SUMMARY.md`
</output>
