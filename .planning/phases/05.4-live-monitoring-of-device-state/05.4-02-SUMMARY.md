---
phase: 05.4-live-monitoring-of-device-state
plan: 02
subsystem: ui
tags: [php, javascript, ajax, polling, jquery-mobile]

requires:
  - phase: 05.4-live-monitoring-of-device-state
    provides: LQI column and Blinds tab with z2m state per device
provides:
  - AJAX endpoint returning device status as JSON
  - Client-side 30s polling with automatic table rebuild
  - Live-updating Blinds tab with position/state/motor_reversal
affects: []

tech-stack:
  added: []
  patterns:
    - "resolve_z2m_path() helper extracts z2m path resolution for reuse"
    - "innerHTML rebuild from JSON with escapeAttr for safe HTML attribute encoding"
    - "Polling pauses on document.hidden and Settings tab active"

key-files:
  created: []
  modified:
    - webfrontend/htmlauth/index.php

key-decisions:
  - "get_status_data handler mirrors PHP table_rows logic to produce identical JSON structure"
  - "Polling skips when Settings tab active (tab index 0) to avoid unnecessary requests"
  - "innerHTML rebuild approach chosen over DOM diffing for simplicity; applyFilters() re-applied after each rebuild"

patterns-established:
  - "AJAX POST handler pattern: read config fresh, build data, return JSON, exit"

requirements-completed: []

duration: 2min
completed: 2026-03-17
---

# Phase 5.4 Plan 02: AJAX Polling for Live Device Status Summary

**30-second AJAX polling with server-side JSON endpoint and client-side table rebuild for automatic device status updates**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-17T12:31:27Z
- **Completed:** 2026-03-17T12:33:30Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Extracted resolve_z2m_path() helper function, replacing inline z2m path resolution
- Added get_status_data POST handler returning full device data as JSON
- Added updateStatusTable() and updateBlindsTable() JS functions for DOM rebuild from JSON
- Added 30s polling with document.hidden and Settings tab skip logic
- Added escapeAttr() and badgeColor() JS helpers for safe HTML generation

## Task Commits

Each task was committed atomically:

1. **Task 1: Add AJAX JSON endpoint and z2m path helper** - `2c48143` (feat)
2. **Task 2: Add client-side AJAX polling and table rebuild JS** - `a25982f` (feat)

## Files Created/Modified
- `webfrontend/htmlauth/index.php` - resolve_z2m_path() helper, get_status_data AJAX handler, updateStatusTable/updateBlindsTable JS, polling logic with 30s interval

## Decisions Made
- get_status_data handler mirrors the PHP $table_rows build logic to ensure JSON structure matches server-rendered HTML exactly
- Polling skips when Settings tab is active (tab index 0) to avoid unnecessary server requests
- innerHTML rebuild with applyFilters() call after each update preserves search/show-excluded state
- Z2M state tooltip popup works on rebuilt rows via existing jQuery event delegation

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Live polling is operational; device status and blinds tables update automatically
- Ready for UAT verification on Loxberry host

---
*Phase: 05.4-live-monitoring-of-device-state*
*Completed: 2026-03-17*
