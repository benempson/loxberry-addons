---
phase: 03-alert-delivery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/loxberry-notify.js
  - bin/lib/email-notify.js
  - bin/lib/config.js
  - tests/loxberry-notify.test.js
  - tests/email-notify.test.js
  - package.json
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02

must_haves:
  truths:
    - "Loxberry notifications are sent via shell command when loxberry_enabled is true"
    - "SMTP email is sent via Nodemailer when email_enabled is true"
    - "Shell injection is prevented in Loxberry notification messages"
    - "SMTP TLS is auto-detected based on port (465=direct TLS, 587=STARTTLS, 25=plain)"
    - "Nodemailer transport has connection/greeting/socket timeouts to avoid hitting 30s hard timeout"
    - "Config supports heartbeat_enabled boolean field"
  artifacts:
    - path: "bin/lib/loxberry-notify.js"
      provides: "Loxberry notification channel via shell"
      exports: ["sendLoxberryNotification"]
    - path: "bin/lib/email-notify.js"
      provides: "SMTP email notification channel via Nodemailer"
      exports: ["sendEmailNotification"]
    - path: "tests/loxberry-notify.test.js"
      provides: "Loxberry notify unit tests"
    - path: "tests/email-notify.test.js"
      provides: "Email notify unit tests"
  key_links:
    - from: "bin/lib/loxberry-notify.js"
      to: "child_process.execSync"
      via: "shell command to notify.sh"
      pattern: "execSync"
    - from: "bin/lib/email-notify.js"
      to: "nodemailer"
      via: "createTransport + sendMail"
      pattern: "createTransport|sendMail"
    - from: "bin/lib/config.js"
      to: "NOTIFICATIONS.heartbeat_enabled"
      via: "BOOLEAN_FIELDS addition"
      pattern: "heartbeat_enabled"
---

<objective>
Create the two notification delivery channel modules -- Loxberry shell notifications and SMTP email via Nodemailer. Also extend config.js with heartbeat_enabled field.

Purpose: These are the I/O modules that actually deliver notifications. Each operates independently (if one fails, the other still sends). Plan 03 wires them together via the dispatcher.
Output: Two tested channel modules + Nodemailer dependency installed + config extended.
</objective>

<execution_context>
@C:/Users/Ben/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-alert-delivery/03-CONTEXT.md
@.planning/phases/03-alert-delivery/03-RESEARCH.md

<interfaces>
<!-- Config shape from bin/lib/config.js -->
From bin/lib/config.js:
```javascript
config.NOTIFICATIONS = {
  loxberry_enabled: boolean,   // coerced from '0'/'1'
  email_enabled: boolean,      // coerced from '0'/'1'
  smtp_host: string,
  smtp_port: number,           // coerced to int
  smtp_user: string,
  smtp_pass: string,
  smtp_from: string,
  smtp_to: string,
  // heartbeat_enabled: boolean  // TO BE ADDED
};
```

<!-- Loxberry environment -->
```
LBHOMEDIR = process.env.LBHOMEDIR || '/opt/loxberry'
NOTIFY_SCRIPT = `${LBHOMEDIR}/libs/bashlib/notify.sh`
PLUGIN_NAME = 'zigbee_watchdog'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Loxberry notification channel with TDD</name>
  <files>bin/lib/loxberry-notify.js, tests/loxberry-notify.test.js</files>
  <behavior>
    - sendLoxberryNotification(message, severity) shells out to Loxberry notify command via execSync
    - Command format: `. ${NOTIFY_SCRIPT} && notify ${PLUGIN_NAME} watchdog "${escapedMessage}" [err]`
    - severity 'err' appends err flag; default (info) omits it
    - Shell metacharacters in message are sanitized (quotes, backticks, $, backslashes stripped/escaped)
    - execSync called with shell:'/bin/bash', timeout:5000, stdio:'pipe'
    - LBHOMEDIR sourced from env var with fallback to /opt/loxberry
    - Throws on execSync failure (caller handles try/catch)
  </behavior>
  <action>
    Write tests first in tests/loxberry-notify.test.js. Mock child_process.execSync using jest.mock. Verify:
    - Correct command string construction for info and error severity
    - Shell metacharacter sanitization (test with names like `living room "light"`, `sensor$1`, backtick-name)
    - LBHOMEDIR env var override
    - execSync options (shell, timeout, stdio)
    - Error propagation when execSync throws
    Run tests (RED), then implement bin/lib/loxberry-notify.js per 03-RESEARCH.md Pattern 2.
    Sanitize dynamic content with a strict approach: replace double quotes with single quotes, strip backticks and $, escape backslashes.
    Run tests (GREEN).
  </action>
  <verify>
    <automated>npx jest tests/loxberry-notify.test.js -x</automated>
  </verify>
  <done>sendLoxberryNotification constructs correct shell commands with proper sanitization. All execSync calls use correct options. Tests cover info/error severity, special characters in messages, env var override. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SMTP email channel + config extension with TDD</name>
  <files>bin/lib/email-notify.js, tests/email-notify.test.js, bin/lib/config.js</files>
  <behavior>
    - sendEmailNotification(htmlBody, textBody, subject, config) creates Nodemailer transport and sends email
    - Transport uses config.NOTIFICATIONS.smtp_host, smtp_port, smtp_user, smtp_pass
    - TLS: secure=true when port===465, secure=false otherwise (Nodemailer auto-upgrades STARTTLS on 587)
    - Auth object only included when smtp_user is non-empty
    - Transport timeouts: connectionTimeout=10000, greetingTimeout=10000, socketTimeout=10000
    - sendMail called with from=smtp_from, to=smtp_to, subject, html=htmlBody, text=textBody
    - Returns sendMail result (for logging); throws on failure
    - config.js: Add heartbeat_enabled to DEFAULTS.NOTIFICATIONS (default '0') and to BOOLEAN_FIELDS.NOTIFICATIONS
  </behavior>
  <action>
    First, extend bin/lib/config.js: add `heartbeat_enabled: '0'` to DEFAULTS.NOTIFICATIONS and add `'heartbeat_enabled'` to BOOLEAN_FIELDS.NOTIFICATIONS array. This is a one-line addition to each object.

    Then write tests in tests/email-notify.test.js. Mock nodemailer using jest.mock -- mock createTransport to return an object with a sendMail mock that resolves. Verify:
    - Transport creation with correct host/port/auth/secure/timeout options
    - secure=true when port 465, secure=false when port 587 or 25
    - Auth omitted when smtp_user is empty string
    - sendMail called with correct from/to/subject/html/text
    - All three timeout fields set to 10000
    - Error propagation when sendMail rejects
    Run tests (RED), then implement bin/lib/email-notify.js.
    CommonJS module with 'use strict'. Require nodemailer at top. Export sendEmailNotification as async function.
    Run tests (GREEN).

    Install nodemailer: `yarn add nodemailer`
  </action>
  <verify>
    <automated>npx jest tests/email-notify.test.js -x</automated>
  </verify>
  <done>sendEmailNotification creates correct Nodemailer transport with TLS auto-detection and timeouts. Config extended with heartbeat_enabled. Nodemailer installed as dependency. All tests green.</done>
</task>

</tasks>

<verification>
npx jest tests/loxberry-notify.test.js tests/email-notify.test.js -x && npx jest tests/config.test.js -x
</verification>

<success_criteria>
- Loxberry notify shells out with proper command construction and sanitization
- Email notify creates Nodemailer transport with correct TLS/timeout/auth options
- Config has heartbeat_enabled boolean field with default false
- Nodemailer installed in package.json
- Full test suite passes: npx jest
</success_criteria>

<output>
After completion, create `.planning/phases/03-alert-delivery/03-02-SUMMARY.md`
</output>
