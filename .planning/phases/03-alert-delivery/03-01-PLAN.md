---
phase: 03-alert-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/bridge-monitor.js
  - bin/lib/email-template.js
  - tests/bridge-monitor.test.js
  - tests/email-template.test.js
autonomous: true
requirements:
  - ALRT-06
  - NOTF-03

must_haves:
  truths:
    - "Bridge offline state is detected from bridge/state topic message"
    - "Bridge state transitions (online->offline, offline->online) are tracked without duplicate alerts"
    - "When bridge is offline, a transition object is returned to signal notification"
    - "Alert messages include device friendly name, status, and relevant detail"
    - "Email body contains both HTML and plain text versions"
    - "HTML email has color-coded table layout (red offline, amber battery, green recovery)"
  artifacts:
    - path: "bin/lib/bridge-monitor.js"
      provides: "Bridge state checker with transition tracking"
      exports: ["checkBridgeState"]
    - path: "bin/lib/email-template.js"
      provides: "Email body and subject line builders"
      exports: ["buildEmailBody", "buildSubject", "buildLoxberryMessage", "buildHeartbeatBody"]
    - path: "tests/bridge-monitor.test.js"
      provides: "Bridge monitor unit tests"
    - path: "tests/email-template.test.js"
      provides: "Email template unit tests"
  key_links:
    - from: "bin/lib/bridge-monitor.js"
      to: "state.bridge_online, state.bridge_offline_since"
      via: "state mutation"
      pattern: "state\\.bridge_online"
    - from: "bin/lib/email-template.js"
      to: "evaluator transition objects"
      via: "transition.friendly_name, transition.type, transition.detail"
      pattern: "t\\.friendly_name|t\\.type|t\\.detail"
---

<objective>
Create the bridge offline monitor and message formatting modules -- the pure-logic building blocks that Plan 03 wires together.

Purpose: Bridge monitor (ALRT-06) gates device evaluation and produces transition objects. Email template (NOTF-03) formats transitions into HTML/text for both notification channels.
Output: Two tested modules with no I/O dependencies, ready for consumption by the dispatcher.
</objective>

<execution_context>
@C:/Users/Ben/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-alert-delivery/03-CONTEXT.md
@.planning/phases/03-alert-delivery/03-RESEARCH.md

<interfaces>
<!-- Transition objects from evaluator (input to email-template) -->
From bin/lib/evaluator.js:
```javascript
// Each transition object shape:
{
  type: 'offline' | 'battery',
  transition: 'alert' | 'recovery',
  ieee: string,
  friendly_name: string,
  detail: string,       // e.g. "not seen for 25.3 hours" or "battery at 15%"
  timestamp: string,    // ISO 8601
}
```

From bin/lib/mqtt-collector.js:
```javascript
// messages is a Map<string, object>
// bridge/state topic payload: { state: 'online' | 'offline' }
// Topic key: `${baseTopic}/bridge/state`
```

From bin/lib/config.js:
```javascript
// Config shape relevant to this plan:
config.NOTIFICATIONS.heartbeat_enabled  // boolean (needs adding)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bridge monitor module with TDD</name>
  <files>bin/lib/bridge-monitor.js, tests/bridge-monitor.test.js</files>
  <behavior>
    - checkBridgeState(messages, baseTopic, state, now) returns transition object on state change, null otherwise
    - Online->offline transition: sets state.bridge_online=false, state.bridge_offline_since=ISO timestamp, returns {type:'bridge', transition:'offline'}
    - Offline->online transition: sets state.bridge_online=true, clears bridge_offline_since, returns {type:'bridge', transition:'online', detail: offlineSince}
    - No change (online->online or offline->offline): returns null, no state mutation beyond bridge_online confirmation
    - Missing bridge/state message (not in Map): treated as offline
    - First run (state.bridge_online undefined): defaults to assuming bridge was online (wasOnline=true)
    - Non-JSON or missing state field in payload: treated as offline
  </behavior>
  <action>
    Write tests first in tests/bridge-monitor.test.js covering all behaviors above.
    Run tests -- they must fail (RED).
    Then implement bin/lib/bridge-monitor.js following the pattern from 03-RESEARCH.md Pattern 3.
    CommonJS module with 'use strict', single export: checkBridgeState.
    Function signature: checkBridgeState(messages, baseTopic, state, now) where messages is a Map, baseTopic is string, state is mutable object, now is injectable Date.
    Run tests -- they must pass (GREEN).
  </action>
  <verify>
    <automated>npx jest tests/bridge-monitor.test.js -x</automated>
  </verify>
  <done>checkBridgeState correctly detects all bridge state transitions, handles missing/malformed payloads, and mutates state.bridge_online + state.bridge_offline_since appropriately. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Email template and message formatting with TDD</name>
  <files>bin/lib/email-template.js, tests/email-template.test.js</files>
  <behavior>
    - buildEmailBody(transitions) returns { html, text } with alerts section and recoveries section
    - HTML contains table with Device, Status, Detail columns; red (#e74c3c) for offline, amber (#f39c12) for battery, green (#27ae60) for recovery
    - Plain text contains "NEW ALERTS" and "RECOVERIES" headers with indented device lines
    - Empty alerts or recoveries section is omitted (not an empty table)
    - HTML-escapes device names and details (prevents injection from names containing angle brackets etc)
    - buildSubject(transitions) returns "Zigbee Watchdog: N alerts, N recovery" (count-based per locked decision)
    - buildLoxberryMessage(transitions) returns plain text formatted message with ALERTS and RECOVERIES sections
    - buildHeartbeatBody(summary) returns { html, text, subject } with "All clear: N devices tracked, 0 alerts, N excluded"
    - Handles edge case: single alert (no plural "s"), single recovery
  </behavior>
  <action>
    Write tests first in tests/email-template.test.js covering all behaviors above. Use transition objects matching the evaluator shape (type, transition, friendly_name, detail, ieee, timestamp).
    Run tests -- they must fail (RED).
    Then implement bin/lib/email-template.js following the patterns from 03-RESEARCH.md.
    CommonJS module with 'use strict'. Exports: buildEmailBody, buildSubject, buildLoxberryMessage, buildHeartbeatBody.
    buildEmailBody: HTML table layout with inline styles (email clients strip external CSS). Color-code rows by type. Include both html and text fields.
    buildSubject: Count alerts and recoveries from transitions array.
    buildLoxberryMessage: Plain text only, suitable for Loxberry notification system.
    buildHeartbeatBody: "All clear" message with device count summary from evaluation summary object.
    HTML escape helper (esc) for all interpolated strings -- covers &, <, >, " characters.
    Run tests -- they must pass (GREEN).
  </action>
  <verify>
    <automated>npx jest tests/email-template.test.js -x</automated>
  </verify>
  <done>All four formatting functions produce correct output. HTML contains color-coded tables with proper escaping. Plain text is readable. Subject lines match locked format. Heartbeat message includes device count summary. All tests green.</done>
</task>

</tasks>

<verification>
npx jest tests/bridge-monitor.test.js tests/email-template.test.js -x
</verification>

<success_criteria>
- checkBridgeState handles all transition cases and edge cases (missing payload, first run, no change)
- buildEmailBody produces HTML with color-coded table + plain text alternative
- buildSubject produces count-based subject lines per locked decision
- buildLoxberryMessage produces plain text for Loxberry notifications
- buildHeartbeatBody produces "all clear" summary
- All strings properly HTML-escaped
- Both modules have comprehensive test coverage
- Full test suite passes: npx jest
</success_criteria>

<output>
After completion, create `.planning/phases/03-alert-delivery/03-01-SUMMARY.md`
</output>
