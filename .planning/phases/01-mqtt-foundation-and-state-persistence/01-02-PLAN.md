---
phase: 01-mqtt-foundation-and-state-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - bin/lib/mqtt-collector.js
  - bin/lib/device-registry.js
  - tests/mqtt-collector.test.js
  - tests/device-registry.test.js
autonomous: true
requirements:
  - MQTT-01
  - MQTT-02
  - MQTT-03
  - MQTT-04
  - DEVT-01

must_haves:
  truths:
    - "MQTT collector connects to broker with configurable host, port, username, password"
    - "MQTT collector subscribes to configurable base topic with multi-level wildcard"
    - "MQTT collector waits for drain window then disconnects cleanly"
    - "Hard process timeout fires after 30s as safety net"
    - "Device registry parses bridge/devices into Map keyed on IEEE address"
    - "Coordinator and incomplete-interview devices are filtered out of registry"
  artifacts:
    - path: "bin/lib/mqtt-collector.js"
      provides: "MQTT connect, subscribe, drain, disconnect lifecycle"
      exports: ["collectMessages"]
    - path: "bin/lib/device-registry.js"
      provides: "Parse bridge/devices payload into device Map"
      exports: ["buildDeviceRegistry"]
    - path: "tests/mqtt-collector.test.js"
      provides: "Unit tests for MQTT collector with mocked mqtt client"
    - path: "tests/device-registry.test.js"
      provides: "Unit tests for device registry parsing"
  key_links:
    - from: "bin/lib/mqtt-collector.js"
      to: "mqtt"
      via: "mqtt.connect() with config options"
      pattern: "mqtt\\.connect"
    - from: "bin/lib/device-registry.js"
      to: "bridge/devices payload"
      via: "Array filter and Map construction"
      pattern: "ieee_address"
---

<objective>
Implement the MQTT collector module (connect, subscribe, drain window, clean disconnect) and the device registry parser (bridge/devices to IEEE-keyed Map).

Purpose: These are the two data-acquisition modules. The MQTT collector gets raw messages from the broker. The device registry transforms the bridge/devices payload into the canonical device map that all downstream logic uses. Both must work correctly before the main entry point can wire them together.

Output: Two tested modules -- collectMessages() and buildDeviceRegistry() -- ready for integration.
</objective>

<execution_context>
@C:/Users/Ben/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-mqtt-foundation-and-state-persistence/01-RESEARCH.md
@.planning/phases/01-mqtt-foundation-and-state-persistence/01-01-SUMMARY.md

<interfaces>
From bin/lib/config.js (created in plan 01):
```javascript
// readConfig returns:
// {
//   MQTT: { host: string, port: number, base_topic: string, username: string, password: string },
//   THRESHOLDS: { offline_hours: number, battery_pct: number },
//   CRON: { interval_minutes: number, drain_seconds: number },
//   NOTIFICATIONS: { loxberry_enabled: boolean, email_enabled: boolean, smtp_host: string, ... },
//   EXCLUSIONS: { devices: string[] }
// }
module.exports = { readConfig };
```

From tests/fixtures/bridge-devices.json (created in plan 01):
Sample zigbee2mqtt bridge/devices array with Coordinator, Router, EndDevice entries.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Device registry module -- parse bridge/devices into IEEE-keyed Map</name>
  <files>bin/lib/device-registry.js, tests/device-registry.test.js</files>
  <behavior>
    - buildDeviceRegistry(payload) returns a Map keyed on ieee_address
    - Each Map value contains: friendly_name, power_source, type, model_id, supported
    - Coordinator devices (type === "Coordinator") are excluded
    - Devices with interview_completed !== true are excluded
    - Non-array input returns empty Map
    - null/undefined input returns empty Map
    - Empty array returns empty Map
    - Devices with missing ieee_address are skipped
  </behavior>
  <action>
RED: Create `tests/device-registry.test.js`. Load `tests/fixtures/bridge-devices.json` as the primary test fixture. Write tests:
1. Parses fixture and returns expected device count (excluding Coordinator + incomplete interview)
2. Each entry has correct fields (friendly_name, power_source, type, model_id, supported)
3. Coordinator is not in result
4. Incomplete interview device is not in result
5. Returns empty Map for null, undefined, non-array, empty array
Run tests -- must fail.

GREEN: Create `bin/lib/device-registry.js` exporting `buildDeviceRegistry(bridgeDevicesPayload)`:
- Guard: if not Array.isArray(payload), return new Map()
- Iterate payload, skip if type === "Coordinator" or !interview_completed or !ieee_address
- Set map entry: ieee_address -> { friendly_name, power_source, type, model_id: device.model_id || null, supported: device.supported !== false }
- Return Map
Run tests -- must pass.
  </action>
  <verify>
    <automated>cd "F:/Arrayx/Clients/Arrayx/loxberry-addons" && yarn test tests/device-registry.test.js 2>&1 | tail -10</automated>
  </verify>
  <done>All device-registry tests pass. buildDeviceRegistry correctly filters Coordinator and incomplete devices, returns Map keyed on IEEE address with correct fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MQTT collector module -- connect, subscribe, drain, disconnect</name>
  <files>bin/lib/mqtt-collector.js, tests/mqtt-collector.test.js</files>
  <behavior>
    - collectMessages(mqttConfig) connects with host, port, username, password from config
    - Sets reconnectPeriod: 0 and clean: true (critical for cron script)
    - Subscribes to `${base_topic}/#` after connect event
    - Collects messages into a Map keyed by topic, values are parsed JSON
    - Non-JSON payloads are silently skipped (no crash)
    - After drain_seconds, calls client.end() and resolves with collected messages Map
    - Rejects on connection error
    - Rejects on subscribe error
    - connectTimeout is set to 5000ms
  </behavior>
  <action>
RED: Create `tests/mqtt-collector.test.js`. Mock the `mqtt` module using Jest manual mock or jest.mock(). The mock must simulate:
- connect() returning an EventEmitter-like object with on(), subscribe(), end() methods
- Emitting 'connect' event after connect()
- subscribe() calling callback with null (success) or error
- Emitting 'message' events with topic and Buffer payload
- end() calling callback

Write tests:
1. Connects with correct options (host, port, username, password, reconnectPeriod: 0, clean: true, connectTimeout: 5000)
2. Subscribes to `${base_topic}/#` after connect
3. Collects JSON messages into Map by topic
4. Skips non-JSON payloads without crashing
5. Resolves after drain_seconds with collected messages
6. Rejects on connection error
7. Rejects on subscribe error
Run tests -- must fail.

GREEN: Create `bin/lib/mqtt-collector.js` exporting `collectMessages(mqttConfig)`:
- mqttConfig shape: { host, port, base_topic, username, password, drain_seconds }
  (drain_seconds comes from CRON section but is passed in by the caller)
- Call mqtt.connect() with url `mqtt://${host}:${port}` and options: username (or undefined if empty), password (or undefined if empty), connectTimeout: 5000, reconnectPeriod: 0, clean: true
- Return a Promise that:
  - On 'connect': subscribe to `${base_topic}/#` with qos: 0
  - On 'message': try JSON.parse(payload.toString()), set in Map by topic, catch and skip non-JSON
  - After drain timeout (drain_seconds * 1000): call client.end(false, callback), resolve with Map
  - On 'error': call client.end(true), reject
  - On subscribe error: reject
Run tests -- must pass.

NOTE: The mqttConfig parameter should accept drain_seconds directly. The caller (watchdog.js in plan 04) will merge config.MQTT with config.CRON.drain_seconds before calling.
  </action>
  <verify>
    <automated>cd "F:/Arrayx/Clients/Arrayx/loxberry-addons" && yarn test tests/mqtt-collector.test.js 2>&1 | tail -10</automated>
  </verify>
  <done>All mqtt-collector tests pass. collectMessages connects with correct options, subscribes to wildcard topic, collects JSON messages during drain window, handles errors gracefully, and disconnects cleanly.</done>
</task>

</tasks>

<verification>
- `yarn test` passes with all device-registry and mqtt-collector tests green
- No test uses real MQTT connections (all mocked)
- Both modules export their primary function
</verification>

<success_criteria>
- buildDeviceRegistry correctly parses bridge/devices, filters Coordinator + incomplete, keys on IEEE address
- collectMessages connects with configurable params, drains for N seconds, returns Map of parsed messages
- Non-JSON payloads don't crash the collector
- reconnectPeriod: 0 and clean: true are set (verified by tests checking connect options)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-mqtt-foundation-and-state-persistence/01-02-SUMMARY.md`
</output>
