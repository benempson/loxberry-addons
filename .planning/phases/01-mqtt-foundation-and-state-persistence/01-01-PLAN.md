---
phase: 01-mqtt-foundation-and-state-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - jest.config.js
  - bin/lib/config.js
  - tests/config.test.js
  - tests/fixtures/watchdog.cfg
  - tests/fixtures/bridge-devices.json
  - tests/fixtures/state.json
autonomous: true
requirements:
  - PLUG-05

must_haves:
  truths:
    - "yarn install succeeds and all dependencies are available"
    - "yarn test runs Jest and exits cleanly (even with zero tests initially)"
    - "config.js reads an INI file and returns a typed config object with defaults merged"
    - "PHP parse_ini_file() produces the same sections and keys as the Node.js ini parser for the same file"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with mqtt, ini, proper-lockfile, write-file-atomic, jest"
    - path: "jest.config.js"
      provides: "Jest configuration"
    - path: "bin/lib/config.js"
      provides: "INI config reader with typed defaults"
      exports: ["readConfig"]
    - path: "tests/config.test.js"
      provides: "Unit tests for config reader"
    - path: "tests/fixtures/watchdog.cfg"
      provides: "Sample INI config file for tests"
    - path: "tests/fixtures/bridge-devices.json"
      provides: "Sample bridge/devices payload for tests"
    - path: "tests/fixtures/state.json"
      provides: "Sample state file for tests"
  key_links:
    - from: "bin/lib/config.js"
      to: "tests/fixtures/watchdog.cfg"
      via: "ini.parse reads INI sections"
      pattern: "ini\\.parse"
---

<objective>
Initialize the project scaffold (package.json, Jest, dependencies) and implement the INI config reader module with test fixtures.

Purpose: Every subsequent plan depends on installed dependencies, a working test runner, and shared test fixtures. The config reader is the first module -- it has zero internal dependencies and validates the INI format that both Node.js and PHP must share (PLUG-05).

Output: Working project with `yarn test` green, config.js module, and test fixtures used by all later plans.
</objective>

<execution_context>
@C:/Users/Ben/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-mqtt-foundation-and-state-persistence/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffold -- package.json, dependencies, Jest config, and test fixtures</name>
  <files>package.json, jest.config.js, tests/fixtures/watchdog.cfg, tests/fixtures/bridge-devices.json, tests/fixtures/state.json</files>
  <action>
1. Create `package.json` with:
   - name: "zigbee-watchdog"
   - version: "0.1.0"
   - private: true
   - main: "bin/watchdog.js"
   - scripts: { "test": "jest", "test:coverage": "jest --coverage" }
   - dependencies: mqtt@^5.15.0, ini@^5.0.0, proper-lockfile@^4.1.2, write-file-atomic@^7.0.0
   - devDependencies: jest@^29.0.0

2. Create `jest.config.js`:
   ```js
   module.exports = { testEnvironment: 'node', testMatch: ['**/tests/**/*.test.js'] };
   ```

3. Run `yarn install` to generate yarn.lock and node_modules.

4. Create `tests/fixtures/watchdog.cfg` -- sample INI config matching the format from research:
   ```ini
   [MQTT]
   host = localhost
   port = 1883
   base_topic = zigbee2mqtt
   username =
   password =

   [THRESHOLDS]
   offline_hours = 24
   battery_pct = 25

   [CRON]
   interval_minutes = 60
   drain_seconds = 3

   [NOTIFICATIONS]
   loxberry_enabled = 1
   email_enabled = 0
   smtp_host =
   smtp_port = 587
   smtp_user =
   smtp_pass =
   smtp_from =
   smtp_to =

   [EXCLUSIONS]
   devices =
   ```

5. Create `tests/fixtures/bridge-devices.json` -- sample zigbee2mqtt bridge/devices payload containing:
   - 1 Coordinator device (type: "Coordinator") -- should be filtered out
   - 1 Router device (mains-powered, interview_completed: true)
   - 2 EndDevice devices (battery-powered, interview_completed: true)
   - 1 device with interview_completed: false -- should be filtered out
   Each device must have: ieee_address, friendly_name, power_source, type, interview_completed, model_id, supported fields. Use realistic zigbee2mqtt payload structure.

6. Create `tests/fixtures/state.json` -- sample state file:
   ```json
   {
     "last_run": "2026-03-14T10:00:00.000Z",
     "devices": {
       "0x00158d0001a2b3c4": {
         "friendly_name": "Living Room Motion",
         "power_source": "Battery",
         "type": "EndDevice",
         "last_seen": "2026-03-14T09:45:00.000Z",
         "battery": 87,
         "alerts": { "offline": false, "offline_sent_at": null, "battery": false, "battery_sent_at": null }
       },
       "0x00158d0001d4e5f6": {
         "friendly_name": "Kitchen Door",
         "power_source": "Battery",
         "type": "EndDevice",
         "last_seen": "2026-03-13T08:00:00.000Z",
         "battery": 12,
         "alerts": { "offline": false, "offline_sent_at": null, "battery": false, "battery_sent_at": null }
       }
     }
   }
   ```
  </action>
  <verify>
    <automated>cd "F:/Arrayx/Clients/Arrayx/loxberry-addons" && yarn test --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>package.json exists with all dependencies, yarn.lock generated, `yarn test` exits 0, all three fixture files exist and are valid (JSON parseable / INI parseable)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: INI config reader module with defaults and type coercion</name>
  <files>bin/lib/config.js, tests/config.test.js</files>
  <behavior>
    - readConfig(path) returns object with sections: MQTT, THRESHOLDS, CRON, NOTIFICATIONS, EXCLUSIONS
    - Missing INI keys are filled from defaults (e.g., base_topic defaults to "zigbee2mqtt")
    - Numeric fields (port, offline_hours, battery_pct, interval_minutes, drain_seconds, smtp_port) are coerced to numbers
    - Boolean fields (loxberry_enabled, email_enabled) are coerced to booleans (1/true -> true, 0/empty -> false)
    - EXCLUSIONS.devices is parsed as comma-separated array (empty string -> empty array)
    - readConfig with nonexistent file throws a clear error (not a cryptic ENOENT)
    - readConfig with empty file returns all defaults
  </behavior>
  <action>
RED: Create `tests/config.test.js` with tests for all behaviors above. Use the fixture `tests/fixtures/watchdog.cfg`. Run tests -- they must fail (config.js doesn't exist yet).

GREEN: Create `bin/lib/config.js` that exports `readConfig(configPath)`:
- Read file with `fs.readFileSync(configPath, 'utf8')`
- Parse with `ini.parse(raw)`
- Define DEFAULTS constant matching research (MQTT, THRESHOLDS, CRON, NOTIFICATIONS, EXCLUSIONS sections)
- Merge parsed over defaults per section: `{ ...defaults[section], ...(parsed[section] || {}) }`
- Coerce numeric fields: parseInt for port, offline_hours, battery_pct, interval_minutes, drain_seconds, smtp_port
- Coerce boolean fields: `val === '1' || val === 'true'` for loxberry_enabled, email_enabled
- Parse EXCLUSIONS.devices: split on comma, trim, filter empty strings
- Wrap fs.readFileSync in try/catch -- rethrow with descriptive message including path
- Run tests -- they must pass.

REFACTOR if needed. Ensure readConfig is the single named export.
  </action>
  <verify>
    <automated>cd "F:/Arrayx/Clients/Arrayx/loxberry-addons" && yarn test tests/config.test.js 2>&1 | tail -10</automated>
  </verify>
  <done>All config tests green. readConfig returns typed config object with defaults, coercion, and error handling. Module exports readConfig.</done>
</task>

</tasks>

<verification>
- `yarn test` passes with all config tests green
- `node -e "const c = require('./bin/lib/config'); console.log(JSON.stringify(c.readConfig('tests/fixtures/watchdog.cfg'), null, 2))"` prints a valid config object with numeric port (1883), boolean loxberry_enabled (true), and empty array for exclusions
</verification>

<success_criteria>
- package.json has all 4 runtime deps and jest dev dep
- yarn.lock exists
- Jest runs and finds tests in tests/ directory
- config.js readConfig() returns typed config with defaults merged
- 3 fixture files exist for use by subsequent plans
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-mqtt-foundation-and-state-persistence/01-01-SUMMARY.md`
</output>
